mkdir -p bin

PROTO_EXTRAS=https://github.com/protocolbuffers/protobuf/archive/master.zip
ZIP="master.zip"

curl -L --insecure -o ${ZIP} ${PROTO_EXTRAS}
unzip -q master.zip -d bin
mv bin/protobuf-master $1
rm ${ZIP}
